# Let Vite and Wrangler select the appropriate environment files.
# Bun preloading .env would override Vite's .env.production values.
env = false

# Miniflare needs the complete npm Undici dispatcher API.
preload = ["./scripts/bun-compat.ts"]
